{{ 'breadcrumbs.css' | asset_url | stylesheet_tag }}
{{ 'photoswipe.css' | asset_url | stylesheet_tag }}
{{ 'main-product.css' | asset_url | stylesheet_tag }}
{{ 'product-card.css' | asset_url | stylesheet_tag }}
<script src="{{ 'product.js' | asset_url }}" defer="defer"></script>
<script type="application/json">
  {{ product | json }}
</script>

{%- style -%}
  .main-product{
    --section-padding-top: {{ section.settings.section_top_padding }}px;
    --section-padding-bottom: {{ section.settings.section_bottom_padding }}px;
  }
{%- endstyle -%}
{%- liquid
  assign js_contents_prefix = 'js-contents--'
  assign section_scheme_class = section.settings.section_color_scheme | prepend: 'scheme scheme--'

  assign product_image_ratio_class = ''
  assign product_image_position_class = ''
  case section.settings.media_aspect_ratio
    when 'portrait'
      assign product_image_ratio_class = '--portrait-media'
      assign product_image_position_class = '--position-media'
    when 'square'
      assign product_image_ratio_class = '--square-media'
      assign product_image_position_class = '--position-media'
    when 'landscape'
      assign product_image_ratio_class = '--landscape-media'
      assign product_image_position_class = '--position-media'
    else
      assign product_image_ratio_class = '--adapt-media'
  endcase
  assign section_media_size_class = product_image_ratio_class

  case section.settings.media_width
    when 'large'
      assign media_section_width = '--media-width-lg'
    when 'small'
      assign media_section_width = '--media-width-sm'
    else
      assign media_section_width = '--media-width-md'
  endcase

  if product.selected_or_first_available_variant.featured_media
    assign seo_media = product.selected_or_first_available_variant.featured_media
  else
    assign seo_media = product.featured_media
  endif
  
  capture loading_attr
    if section.index > 1
      echo 'lazy'
      else
        echo 'eager'
    endif
  endcapture

  capture enable_swatches
    unless settings.enable_variant_swatches == 'none'
      echo 'true'
    endunless
  endcapture

  capture is_color_option
    for option in product.options_with_values
      if option.name == 'Color' or option.name == 'color' or option.name == 'Colour' or option.name == 'colour'
        echo 'true'
        break
      endif
    endfor
  endcapture
-%}
<div hidden role="alert" class="error-toast"></div>
<div
  id="{{ section.id }}"
  data-section="{{ section.id }}"
>
  {%- assign first_3d_model = product.media | where: 'media_type', 'model' | first -%}
  {%- if first_3d_model -%}
    <link
      id="ModelViewerStyle"
      rel="stylesheet"
      href="https://cdn.shopify.com/shopifycloud/model-viewer-ui/assets/v1.0/model-viewer-ui.css"
      media="print"
      onload="this.media='all'"
    >
  {%- endif -%}
  <div class="product main-product {{ section_scheme_class }} {{ product_image_position_class }}">
    <div class="container">
      {%- render 'element-breadcrumb' -%}
      <product-card
        data-id="product-card-organizer"
        data-variant-show-case="radio"
        data-prefix="{{ js_contents_prefix }}"
        data-url="{{ product.url }}"
        data-update-url="true"
        {% if settings.enable_variant_swatches == 'images' %}
          data-swatch-images="true"
        {% endif %}
        class="main-product-card"
        {% if section.settings.gallery_view == 'full_two_thumbs' or section.settings.gallery_view == 'full_thumb' %}
          data-media-gallery="stacked"
        {% endif %}
      >
        <div class="main-product_wrapper {{ media_section_width }} {% if product.featured_media == null %}main-product--no-media{% endif %}">
          {% unless product.featured_media == null %}
            <div class="products_gallery {{ section_media_size_class }}">
              <a class="skip-to-content-link visually-hidden" href="#main-product-info">
                {{ 'sections.products.product.skip_to_product_info' | t }}
              </a>
              <div class="main-product_sticky-block {% if section.settings.sticky_product %} sticky {% endif %}">
                {%- liquid
                  case section.settings.gallery_view
                    when 'slider_with_thumbs'
                      render 'product-gallery-slider', product: product, js_contents_prefix: js_contents_prefix, xr_button: true
                    when 'full_two_thumbs'
                      render 'product-gallery-thumbnails', product: product, js_contents_prefix: js_contents_prefix, xr_button: true
                    when 'full_thumb'
                      render 'product-gallery-thumbnails', product: product, flag: 'full_width', js_contents_prefix: js_contents_prefix, xr_button: true
                    when 'slider_full'
                      render 'product-gallery-slider', product: product, flag: 'full_width', js_contents_prefix: js_contents_prefix, xr_button: true
                  endcase
                -%}
              </div>
            </div>
          {% endunless %}
          <div class="products_info" id="main-product-info">
            <div class="main-product_sticky-block {% if section.settings.sticky_product %}sticky{% endif %}">
              {%- liquid
                assign product_form_id = section.id | append: product.id
                
                if product.selected_or_first_available_variant.inventory_management == 'shopify'
                  assign backordered = false
                  if product.selected_or_first_available_variant.inventory_policy == 'continue'
                    assign inventory_text = 'sections.products.product.pre_text' | t
                    assign backordered = true
                    assign stock_bar_text = 'sections.products.product.bckorder_text' | t
                    else
                      assign inventory_count = product.selected_or_first_available_variant.inventory_quantity
                      assign inventory_container_class = '--no-stock'
                      assign inventory_text = 'sections.products.product.sold_out' | t
                      assign stock_bar_text = 'sections.products.product.alas_text' | t
                      assign stock_bar_width = inventory_count | divided_by: 1.00
                      assign stock_bar_color = 'var(--section-error-color)'

                      if stock_bar_width > 0
                        assign stock_bar_color = 'var(--section-warning-color)'
                        assign stock_bar_text = 'sections.products.product.hurry_text' | t
                        assign inventory_container_class = '--low-stock'
                        assign inventory_text = 'sections.products.product.low_stock_text' | t
                      endif

                      if stock_bar_width > 50.0
                        assign stock_bar_color = 'var(--section-success-color)'
                        assign stock_bar_text = 'sections.products.product.excel_text' | t
                        assign inventory_container_class = '--enough-stock'
                        assign inventory_text = 'sections.products.product.in_stock_text' | t
                      endif

                      if stock_bar_width < 1.0
                        assign stock_bar_width = 100.0
                      endif
                  endif
                endif
              -%}
              {%- for block in section.blocks -%}
                {%- case block.type -%}
                  {%- when '@app' -%}
                    {% render block %}
                  {%- when 'custom_liquid' -%}
                    {%- style -%}
                      .main-product_{{ block.type }}-block--{{ forloop.index }}{
                        --block-padding-top: {{ block.settings.block_top_padding }}px;
                        --block-padding-bottom: {{ block.settings.block_bottom_padding }}px;
                      }
                    {%- endstyle -%}
                    <div
                      class="mty main-product_type-block main-product_{{ block.type }}-block--{{ forloop.index }}"
                      {{ block.shopify_attributes }}
                    >
                      <div class="main-product_{{ block.type }}-block">
                        {{ block.settings.custom_liquid }}
                      </div>
                    </div>
                  {%- when 'text' -%}
                    {%- style -%}
                      .main-product_{{ block.type }}-block--{{ forloop.index }}{
                        --block-padding-top: {{ block.settings.block_top_padding }}px;
                        --block-padding-bottom: {{ block.settings.block_bottom_padding }}px;
                      }
                    {%- endstyle -%}
                    <div
                      class="mty main-product_type-block main-product_{{ block.type }}-block--{{ forloop.index }} {% if block.settings.text == product.vendor %} main-product_vendor-name{% endif %}"
                      {{ block.shopify_attributes }}
                    >
                      <div class="main-product_{{ block.type }}-block">
                        <p class="mty">
                          {{ block.settings.text }}
                        </p>
                      </div>
                    </div>
                  {%- when 'title' -%}
                    {%- style -%}
                      .main-product_{{ block.type }}-block{
                        --block-padding-top: {{ block.settings.block_top_padding }}px;
                        --block-padding-bottom: {{ block.settings.block_bottom_padding }}px;
                      }
                    {%- endstyle -%}
                    <div
                      class="mty main-product_type-block main-product_{{ block.type }}-block"
                      {{ block.shopify_attributes }}
                    >
                      {%- render 'element-heading' | elem_heading_tag: 'h1' | elem_heading_parent: 'main-product' | elem_heading_text: product.title | elem_heading_size: block.settings.title_size | elem_heading_custom_class: 'mty' -%}
                    </div>
                  {%- when 'statistics_bar' -%}
                    {%- style -%}
                      .main-product_{{ block.type }}-block{
                        --block-padding-top: {{ block.settings.block_top_padding }}px;
                        --block-padding-bottom: {{ block.settings.block_bottom_padding }}px;
                      }
                    {%- endstyle -%}
                    <div
                      class="mty main-product_type-block main-product_{{ block.type }}-block"
                      {{ block.shopify_attributes }}
                    >
                      {% if block.settings.show_savings_badge or block.settings.show_inventory or block.settings.show_rating %}
                        <div class="main-product_statistics-col">
                          {% if block.settings.show_savings_badge %}
                            <div data-request-contents="sale-tag">
                              {%- render 'price', product: product, use_variant: true, show_currency_code: settings.show_currency_code | show_discount_tag: true -%}
                            </div>
                          {% endif %}
                          {% if block.settings.show_inventory %}
                            {%- if product.selected_or_first_available_variant.inventory_management == 'shopify' -%}
                              {%- unless product.selected_or_first_available_variant.inventory_policy == 'continue' -%}
                                  <strong class="product_stock-inventory" data-request-contents="inventory">
                                    <span class="">
                                      {{ inventory_text }}
                                    </span>
                                  </strong>
                              {%- endunless -%}
                            {% else %}
                              <strong class="product_stock-inventory hidden" data-request-contents="inventory"></strong>
                            {%- endif -%}
                          {% endif %}
                          {% if block.settings.show_rating %}
                            {% liquid
                              assign rating_decimal = 0
                              assign rating_value = product.metafields.reviews.rating.value.rating | plus: 0
                              assign rating_totals = 5 | plus: 0
                              assign decimal = rating_value | modulo: 1

                              if decimal >= 0.3 and decimal <= 0.7
                                assign rating_decimal = 0.5
                              elsif decimal > 0.7
                                assign rating_decimal = 1
                              endif
                            %}
                            <div class="main-product_rating-wrap {% if rating_decimal == 0 and rating_value < 1 %} hidden{% endif %}">
                              <strong aria-hidden="true">
                                {% for star in (1..rating_totals) %}
                                  {% if rating_value >= forloop.index %}
                                    <i class="product_rating-star --active">
                                      {%- render 'icons-data', width: 12, height: 12, use_icon: true, icon_reference: 'star-filled' -%}
                                    </i>
                                  {% else %}
                                    <i class="product_rating-star">
                                      {%- render 'icons-data', width: 12, height: 12, use_icon: true, icon_reference: 'star-filled' -%}
                                    </i>
                                  {% endif %}
                                {% endfor %}
                                <span>
                                  <span class="visually-hidden">
                                    {{- product.metafields.reviews.rating_count | round }} /
                                    {{ product.metafields.reviews.rating.value.scale_max -}}
                                  </span>
                                  <span aria-hidden="true"
                                    >{{- 'additions.product.reviews' | t: reviews_cout: product.metafields.reviews.rating_count -}}</span
                                  >
                                </span>
                              </strong>
                            </div>
                          {% endif %}
                        </div>
                      {% endif %}
                      {% if block.settings.enable_compare_button or block.settings.enable_share %}
                        {{ 'share-button.css' | asset_url | stylesheet_tag }}
                        <div class="main-product_statistics-col --right">
                          <ul class="list-unstyled main-product_actions-list">
                            {% if block.settings.enable_compare_button %}
                              <li>
                                <product-compare-button
                                  data-product="{{ product.title | escape }}"
                                  data-handle="{{ product.handle }}"
                                >
                                  <a href="#" role="button" data-title="{{- 'additions.product.compare.label' | t -}}" class="compare-btn">
                                    <span class="visually-hidden pwb-text">
                                      {{-
                                        'sections.products.product.comparelist.add'
                                        | t: product: product.title
                                        | escape
                                      -}}
                                    </span>
                                    {%- render 'icons-data', width: 20, height: 20, use_icon: true, icon_reference: 'compare' -%}
                                  </a>
                                </product-compare-button>
                              </li>
                            {% endif %}
                            {% if block.settings.enable_share %}
                              <li>
                                {% assign share_url = product.selected_variant.url
                                  | default: product.url
                                  | prepend: request.origin
                                %}
                                {% render 'share-button', prefix: js_contents_prefix, share_link: share_url %}
                              </li>
                            {% endif %}
                          </ul>
                        </div>
                      {% endif %}
                    </div>
                  {%- when 'description' -%}
                    {%- style -%}
                      .main-product_{{ block.type }}-block{
                        --block-padding-top: {{ block.settings.block_top_padding }}px;
                        --block-padding-bottom: {{ block.settings.block_bottom_padding }}px;
                      }
                    {%- endstyle -%}
                    {%- if product.description != blank -%}
                      <div
                        class="mty main-product_type-block main-product_{{ block.type }}-block"
                        {{ block.shopify_attributes }}
                      >
                        {{ product.description }}
                      </div>
                    {%- endif -%}
                  {%- when 'stock_statistics_bar' -%}
                    {%- style -%}
                      .main-product_{{ block.type }}-block{
                        --block-padding-top: {{ block.settings.block_top_padding }}px;
                        --block-padding-bottom: {{ block.settings.block_bottom_padding }}px;
                      }
                    {%- endstyle -%}
                    <div
                      class="mty main-product_type-block main-product_{{ block.type }}-block"
                      {{ block.shopify_attributes }}
                    >
                      <div class="{{ block.type }}-holder">
                        {%- if product.selected_or_first_available_variant.inventory_management == 'shopify' -%}
                          <div class="main-product_stock-col" data-request-contents="stockbar">
                            <div class="stock-col_wrapper">
                              <strong class="main-product_stock-text">
                                {%- render 'icons-data', width: 20, height: 20, use_icon: true, icon_reference: 'document' -%}
                                <span class="{{ inventory_container_class }}">
                                  {{ stock_bar_text }}
                                </span>
                              </strong>
                              {% if backordered == false %}
                                {% if inventory_count < 1 %}
                                  {{ 'sections.products.product.no_stock_text' | t }}
                                {% else %}
                                  {% unless inventory_count > 50 or inventory_count == 0 %} {{ 'sections.products.product.only_stock_text' | t }} {% endunless %}
                                  {{ 'sections.products.product.items_left_stock_text' | t: count: inventory_count }}
                                {% endif %}
                              {% endif %}
                            </div>
                            <span class="main-product_stock-progress {% if backordered %}hidden{% endif %}">
                              <span
                                style="width: {{ stock_bar_width }}%; --stock-bar-bg-color: {{ stock_bar_color }};"
                              ></span>
                            </span>
                          </div>
                        {% else %}
                          <div class="main-product_stock-col hidden" data-request-contents="stockbar"></div>
                        {% endif %}
                        {% if block.settings.enable_sale_timer and block.settings.sale_end_date != blank %}
                          <div class="main-product_stock-col --right">
                            <div class="main-product_sale-countdown">
                              {%- render 'element-timer' | elem_timer_date: block.settings.sale_end_date, | elem_time_message: block.settings.end_message -%}
                            </div>
                          </div>
                        {% endif %}
                      </div>
                    </div>
                  {%- when 'price_and_variations' -%}
                    {%- style -%}
                      .main-product_{{ block.type }}-block{
                        --block-padding-top: {{ block.settings.block_top_padding }}px;
                        --block-padding-bottom: {{ block.settings.block_bottom_padding }}px;
                      }
                    {%- endstyle -%}
                    <div
                      class="mty main-product_type-block main-product_{{ block.type }}-block"
                      {{ block.shopify_attributes }}
                    >
                      <ul class="list-unstyled main-product_paver-list" id="main-product-add-cart-ref">
                        <li class="mty">
                          <strong class="paver-list_title">{{- 'sections.cart.headings.price' | t -}}:</strong>
                          <div class="paver-list_description">
                            <div data-request-contents="price">
                              {%- render 'price',
                                product: product,
                                use_variant: true,
                                show_currency_code: settings.show_currency_code
                              -%}
                            </div>
                            <span class="unavailable-price">{{ 'sections.products.product.unavailable' | t }}</span>
                            {%- if cart.taxes_included or shop.shipping_policy.body != blank -%}
                              <small class="product_tax caption mty">
                                {%- if cart.taxes_included -%}
                                  {{ 'sections.products.product.include_taxes' | t }}
                                {%- endif -%}
                                {%- if shop.shipping_policy.body != blank -%}
                                  {{ 'sections.products.product.shipping_policy_html' | t: link: shop.shipping_policy.url }}
                                {%- endif -%}
                              </small>
                            {%- endif -%}
                          </div>
                        </li>
                        <li class="mty unit-price-wrapper" data-request-contents="unit-price">
                          {%- unless product.selected_or_first_available_variant.unit_price_measurement == null -%}
                            <strong class="paver-list_title">{{- 'additions.product.price_unit' | t -}}:</strong>
                            <div class="paver-list_description">
                              {%- comment -%} Unit price {%- endcomment -%}
                              <span class="visually-hidden">
                                {{- 'sections.products.product.price.unit_price' | t -}}
                              </span>
                              <span class="unit-price">
                                {%- comment -%} Price {%- endcomment -%}
                                <span>{{- product.selected_or_first_available_variant.unit_price | money -}}</span>
                                {%- comment -%} Separator {%- endcomment -%}
                                <span aria-hidden="true">/</span>
                                <span class="visually-hidden"
                                  >&nbsp;
                                  {{- 'accessibility.unit_price_separator' | t -}}
                                  &nbsp;</span
                                >
                                {%- comment -%} Unit {%- endcomment -%}
                                <span>
                                  {%- if product.selected_or_first_available_variant.unit_price_measurement.reference_value
                                      != 1
                                  -%}
                                    {{- product.selected_or_first_available_variant.unit_price_measurement.reference_value -}}
                                  {%- endif -%}
                                  {{- product.selected_or_first_available_variant.unit_price_measurement.reference_unit -}}
                                </span>
                              </span>
                            </div>
                          {%- endunless -%}
                        </li>
                        {% if block.settings.show_sku %}
                          <li
                            class="mty unit-price-wrapper {% if product.selected_or_first_available_variant.sku.size == 0 or product.selected_or_first_available_variant.sku == blank %} hidden {% endif %}"
                            data-parent="sku"
                          >
                            <strong class="paver-list_title">{{- 'additions.product.sku' | t -}}:</strong>
                            <div class="paver-list_description">
                              <span class="{{ js_contents_prefix }}sku">
                                {{- product.selected_or_first_available_variant.sku -}}
                              </span>
                            </div>
                          </li>
                        {% endif %}
                        {% if block.settings.show_variations %}
                          {%- unless product.has_only_default_variant -%}
                            <li class="mty">
                              <product-card-variants>
                                {%- for option in product.options_with_values -%}
                                  {%- liquid
                                    capture has_color_variant
                                      if option.name == 'color' or option.name == 'Color' or option.name == 'colour' or option.name == 'Colour'
                                        echo 'true'
                                      endif
                                    endcapture
                                  -%}
                                  <fieldset>
                                    <legend class="variant-element_title visually-hidden">
                                      {{ option.name | append: ':' }}
                                    </legend>
                                    <div class="{{ js_contents_prefix }}variant-row variant-row--pills {% if has_color_variant == 'true' and enable_swatches == 'true' %} variant-row--pills-color{% endif %}">
                                      <strong class="variant-element_title" aria-hidden="true">
                                        {{- option.name | append: ':' -}}
                                      </strong>
                                      <div class="variant-options-holder">
                                        {% render 'product-variant-options',
                                          product: product,
                                          option: option,
                                          product_form_id: product_form_id
                                        %}
                                      </div>
                                    </div>
                                  </fieldset>
                                {% endfor %}
                              </product-card-variants>
                            </li>
                          {%- endunless -%}
                        {%- endif -%}
                        {% if block.settings.enable_size_chart %}
                          <li class="mty">
                            <strong class="paver-list_title" id="{{ 'sections.products.product.chart.label_by' | t }}">
                              {{- 'sections.products.product.chart.title' | t }}: </strong>
                            <div class="paver-list_description">
                              <modal-component-toggler>
                                <button
                                  type="button"
                                  data-expanded="false"
                                  data-modal-ref="modal-guide-chart"
                                  aria-haspopup="dialog"
                                  aria-controls="modal-guide-chart"
                                  class="guide-chart-btn"
                                >
                                  {% render 'icons-data', width: 25, height: 25, use_icon: true, icon_reference: 'chart' -%}
                                  <span class="visually-hidden">
                                    {{- 'sections.products.product.chart.title' | t }} </span
                                  >
                                  {{- 'additions.product.guide.label' | t -}}
                                </button>
                              </modal-component-toggler>
                              <modal-component
                                data-modal="modal-guide-chart"
                                data-container="body"
                              >
                                <div
                                  class="modal modal-component modal-component--guide-chart modal-component--centered scheme--{{ section.settings.section_color_scheme }}"
                                  role="dialog"
                                  aria-modal="true"
                                  aria-labelledby="modal-guide-chart-heading"
                                  aria-describedby="modal-guide-chart"
                                  tabindex="-1"
                                >
                                  <div class="modal-component_spacer">
                                    <div class="modal-component_wrapper" id="modal-guide-chart">
                                      <div class="modal-component_body">
                                        <div class="modal-component_head">
                                          <modal-component-toggler>
                                            <button type="button"
                                              data-expanded="true"
                                              data-toggle-attribute="false"
                                              data-modal-ref="modal-guide-chart"
                                              class="modal-component_btn-close"
                                            >
                                              {%- render 'icons-data', width: 14, height: 14, use_icon: true, icon_reference: 'cross' -%}
                                              <span class="visually-hidden">{{ 'accessibility.close' | t }}</span>
                                            </button>
                                          </modal-component-toggler>
                                          <strong id="modal-guide-chart-heading" class="modal-component_heading">{{ 'sections.products.product.chart.title' | t }}</strong>
                                        </div>
                                        <div class="modal-component_content">
                                          <div class="chart-popup_img">
                                            {%- if block.settings.size_chart_image != blank -%}
                                              {{-
                                                block.settings.size_chart_image.src |
                                                image_url: width: 1350 |
                                                image_tag: class: 'image-class', alt: 'size guide', widths: '540, 840', sizes: '(max-width: 767px) 540vw, (max-width: 1199px) 840vw, 1350vw', loading: 'lazy' |
                                                escape
                                              -}}
                                              {%- else -%}
                                                {{- 'lifestyle-1' | placeholder_svg_tag: 'img--placeholder' -}}
                                            {%- endif -%}
                                          </div>
                                        </div>
                                      </div>
                                    </div>
                                  </div>
                                </div>
                              </modal-component>
                            </div>
                          </li>
                        {% endif %}
                      </ul>
                      {% if block.settings.enable_ask_question %}
                        <div class="ask-question-popup">
                          <modal-component-toggler>
                            <button
                              type="button"
                              data-expanded="false"
                              data-modal-ref="{{ section.id }}modal-question-ask"
                              aria-haspopup="dialog"
                              aria-controls="{{ section.id }}modal-question-ask"
                              class="question-ask-btn"
                            >
                              {%- render 'icons-data', width: 20, height: 20, use_icon: true, icon_reference: 'question' -%}
                              {{- 'additions.product.ask_question.label' | t -}}
                            </button>
                          </modal-component-toggler>
                          <modal-component
                            data-modal="{{ section.id }}modal-question-ask"
                            data-container="body"
                          >
                            <div
                              class="modal modal-component modal-component--question-ask modal-component--centered scheme--{{ section.settings.section_color_scheme }}"
                              role="dialog"
                              aria-modal="true"
                              aria-labelledby="{{ section.id }}-modal-question-ask-heading"
                              aria-describedby="{{ section.id }}-modal-question-ask"
                              tabindex="-1"
                            >
                              <div class="modal-component_spacer">
                                <div class="modal-component_wrapper" id="{{ section.id }}-modal-question-ask">
                                  <div class="modal-component_body">
                                    <div class="modal-component_head --align-center">
                                      <modal-component-toggler>
                                        <button type="button"
                                          data-expanded="true"
                                          data-toggle-attribute="false"
                                          data-modal-ref="{{ section.id }}modal-question-ask"
                                          class="modal-component_btn-close"
                                        >
                                          {%- render 'icons-data', width: 14, height: 14, use_icon: true, icon_reference: 'cross' -%}
                                          <span class="visually-hidden">{{ 'accessibility.close' | t }}</span>
                                        </button>
                                      </modal-component-toggler>
                                      <strong id="{{ section.id }}-modal-question-ask-heading" class="modal-component_heading">{{- 'additions.product.ask_question.heading' | t -}}</strong>
                                      <em class="question-popup_steric">{{- 'additions.product.ask_question.required_fields' | t -}}</em>
                                    </div>
                                    <div class="modal-component_content">
                                      {%- form 'contact', id: 'AskQuestion', class: 'isolate' -%}
                                        {%- if form.posted_successfully? -%}
                                          <div
                                            class="form-status form-success-status form__message"
                                            tabindex="-1"
                                            autofocus
                                          >
                                            Thanks for contacting us. We'll get back to you as soon as possible.
                                          </div>
                                        {%- elsif form.errors -%}
                                          <div class="form__message">
                                            <div class="form-status" role="alert" tabindex="-1" autofocus>
                                              Please adjust the following:
                                            </div>
                                          </div>
                                          <ul class="form-status-list list-unstyled">
                                            <li>
                                              <a href="#ContactForm-email" class="link">
                                                {{ form.errors.translated_fields.email | capitalize }}
                                                {{ form.errors.messages.email }}
                                              </a>
                                            </li>
                                          </ul>
                                        {%- endif -%}
                                        <div class="contact__fields">
                                          <div class="field">
                                            <input
                                              class="form-control"
                                              autocomplete="name"
                                              type="text"
                                              id="ContactForm-name"
                                              name="contact[{{ 'templates.contact.form.name' | t }}]"
                                              value="{% if form.name %}{{ form.name }}{% elsif customer %}{{ customer.name }}{% endif %}"
                                              placeholder="{{ 'templates.contact.form.name' | t }}"
                                            >
                                            <label class="visually-hidden" for="ContactForm-name">
                                              {{- 'templates.contact.form.name' | t -}}
                                            </label>
                                          </div>
                                          <div class="field">
                                            <input
                                              type="tel"
                                              id="ContactForm-phone"
                                              class="form-control"
                                              autocomplete="tel"
                                              name="contact[Phone]"
                                              pattern="[0-9\-]*"
                                              value="{% if form.phone %}{{ form.phone }}{% elsif customer %}{{ customer.phone }}{% endif %}"
                                              placeholder="{{ 'templates.contact.form.phone' | t }}"
                                            >
                                            <label class="visually-hidden" for="ContactForm-phone">
                                              {{- 'templates.contact.form.phone' | t -}}
                                            </label>
                                          </div>
                                          <div class="field field--with-error">
                                            <input
                                              autocomplete="email"
                                              type="email"
                                              id="ContactForm-email"
                                              class="field__input"
                                              name="contact[email]"
                                              spellcheck="false"
                                              autocapitalize="off"
                                              value="{% if form.email %}{{ form.email }}{% elsif customer %}{{ customer.email }}{% endif %}"
                                              aria-required="true"
                                              {% if form.errors contains 'email' %}
                                                aria-invalid="true"
                                                aria-describedby="ContactForm-email-error"
                                              {% endif %}
                                              placeholder="{{ 'templates.contact.form.email' | t }}*"
                                            >
                                            <label class="visually-hidden" for="ContactForm-email">
                                              {{ 'templates.contact.form.email' | t -}}
                                              <span aria-hidden="true">*</span>
                                              <span class="visually-hidden">
                                                {{- 'templates.contact.form.req' | t -}}
                                              </span>
                                            </label>
                                            <div class="main-product_results-wrap results-wrap_message">
                                              <div class="main-product_errors-wrap">
                                                {%- if form.errors contains 'email' -%}
                                                  <small class="contact__field-error" id="ContactForm-email-error">
                                                    <span class="visually-hidden">{{ 'accessibility.error' | t }}</span>
                                                    <span>
                                                      {{- form.errors.translated_fields.email | capitalize }}
                                                      {{ form.errors.messages.email -}}
                                                    </span>
                                                  </small>
                                                {%- endif -%}
                                              </div>
                                            </div>
                                          </div>
                                        </div>
                                        <div class="field">
                                          <textarea
                                            rows="10"
                                            id="ContactForm-body"
                                            class="form-control form-control--textarea"
                                            name="contact[Comment]"
                                            placeholder="{{ 'templates.contact.form.comment' | t }}"
                                          >
                                            {{- form.body -}}
                                          </textarea>
                                          <label class="visually-hidden" for="ContactForm-body">
                                            {{ 'templates.contact.form.comment' | t }}
                                          </label>
                                        </div>
                                        <div class="contact__button">
                                          <button type="submit" class="button button--primary">
                                            {{ 'templates.contact.form.send' | t }}
                                          </button>
                                        </div>
                                      {%- endform -%}
                                    </div>
                                  </div>
                                </div>
                              </div>
                            </div>
                          </modal-component>
                        </div>
                      {% endif %}
                    </div>
                  {%- when 'quantity_input' -%}
                    {%- style -%}
                      .main-product_{{ block.type }}-block{
                        --block-padding-top: {{ block.settings.block_top_padding }}px;
                        --block-padding-bottom: {{ block.settings.block_bottom_padding }}px;
                      }
                    {%- endstyle -%}
                    <div
                      class="mty main-product_type-block main-product_{{ block.type }}-block"
                      {{ block.shopify_attributes }}
                    >
                      {%- assign cart_qty = cart
                        | item_count_for_variant: product.selected_or_first_available_variant.id
                      -%}
                      <quantity-input class="quantity">
                        <label for="Quantity-{{ section.id }}-{{ product.id }}" class="visually-hidden">
                          {{- 'sections.products.product.quantity.label' | t -}}
                        </label>
                        <div class="quantity_extra-wrap">
                          <button class="quantity_button disabled" disabled="" name="minus" type="button">
                            <span class="visually-hidden">
                              {{- 'sections.products.product.quantity.decrease' | t: product: product.title | escape -}}
                            </span>
                            {%- render 'icons-data', width: 8, height: 8, use_icon: true, icon_reference: 'minus' -%}
                          </button>
                          <input
                            class="quantity__input"
                            type="number"
                            name="quantity"
                            id="Quantity-{{ section.id }}-{{ product.id }}"
                            data-cart-quantity="{{ cart_qty }}"
                            data-min="{{ product.selected_or_first_available_variant.quantity_rule.min }}"
                            min="{{ product.selected_or_first_available_variant.quantity_rule.min }}"
                            {% if product.selected_or_first_available_variant.quantity_rule.max != null %}
                              data-max="{{ product.selected_or_first_available_variant.quantity_rule.max }}"
                              max="{{ product.selected_or_first_available_variant.quantity_rule.max }}"
                            {% endif %}
                            step="{{ product.selected_or_first_available_variant.quantity_rule.increment }}"
                            value="{{ product.selected_or_first_available_variant.quantity_rule.min }}"
                            aria-label="{{ 'sections.products.product.quantity.input_label' | t: product: product.title | escape }}"
                            form="{{ product_form_id }}"
                          >
                          <button class="quantity_button" name="plus" type="button">
                            <span class="visually-hidden">
                              {{- 'sections.products.product.quantity.increase' | t: product: product.title | escape -}}
                            </span>
                            {%- render 'icons-data', width: 8, height: 8, use_icon: true, icon_reference: 'plus' -%}
                          </button>
                        </div>
                        {% if block.settings.show_subtotals %}
                          <strong class="quantity_qty-subtotal-wrap">
                            <span class="qqsw_title">{{- 'sections.cart.subtotal' | t -}}: </span>
                            <span class="quantity_subtotal">
                              {{ product.selected_or_first_available_variant.price | money }}
                            </span>
                          </strong>
                        {% endif %}
                      </quantity-input>
                      <div class="product-card_error-message hidden --align-center {{ js_contents_prefix }}errors">
                        {% comment %} errors should be rendered here {% endcomment %}
                      </div>
                    </div>
                  {%- when 'buttons' -%}
                    {%- style -%}
                      .main-product_{{ block.type }}-block{
                        --block-padding-top: {{ block.settings.block_top_padding }}px;
                        --block-padding-bottom: {{ block.settings.block_bottom_padding }}px;
                      }
                    {%- endstyle -%}
                    <div
                      class="mty main-product_type-block main-product_{{ block.type }}-block"
                      {{ block.shopify_attributes }}
                    >
                      {%- assign product_form_installment_id = 'product-form-installment-' | append: section.id -%}
                      {%- form 'product',
                        product,
                        data-productid: product.id,
                        id: product_form_installment_id,
                        class: 'installment js-contents--form'
                      -%}
                        <input
                          type="hidden"
                          name="id"
                          data-productid="{{ product.id }}"
                          value="{{ product.selected_or_first_available_variant.id }}"
                        >
                        {{ form | payment_terms }}
                      {%- endform -%}
                      {%- form 'product',
                        product,
                        id: product_form_id,
                        class: 'js-contents--form main-product_submit-from',
                        novalidate: 'novalidate',
                        data-type: 'add-to-cart-form'
                      -%}
                        {%- liquid
                          assign gift_card_recipient_feature_active = false
                          if block.settings.show_gift_card_recipient and product.gift_card?
                            assign gift_card_recipient_feature_active = true
                          endif

                          assign show_dynamic_checkout = false
                          if block.settings.show_dynamic_checkouts and gift_card_recipient_feature_active == false
                            assign show_dynamic_checkout = true
                          endif
                        -%}
                        {%- if gift_card_recipient_feature_active -%}
                          {%- render 'gift-card-recipient-form', product: product, form: form, section: section -%}
                        {%- endif -%}
                        <input
                          type="hidden"
                          name="id"
                          data-productid="{{ product.id }}"
                          value="{{ product.selected_or_first_available_variant.id }}"
                        >
                        {%- liquid
                          assign check_against_inventory = true
                          if product.selected_or_first_available_variant.inventory_management != 'shopify' or product.selected_or_first_available_variant.inventory_policy == 'continue'
                            assign check_against_inventory = false
                          endif
                          if product.selected_or_first_available_variant.quantity_rule.min > product.selected_or_first_available_variant.inventory_quantity and check_against_inventory
                            assign quantity_rule_soldout = true
                          endif
                        -%}
                        <div class="button_form-holder">
                          <button
                            id="ProductSubmitButton-{{ section.id }}-{{ product.id }}"
                            type="submit"
                            name="add"
                            class="button button--product-action button-actions {% if product.selected_or_first_available_variant.available == false or quantity_rule_soldout %} button--secondary {% else %} button--primary {% endif %}"
                            {% if product.selected_or_first_available_variant.available == false
                              or quantity_rule_soldout
                            %}
                              disabled
                            {% endif %}
                          >
                            {%- liquid
                              if settings.show_buttons_icon
                                render 'icons-data', width: 20, height: 20, use_icon: true, icon_reference: 'cart'
                              endif
                            -%}
                            <span class="button--text">
                              {% if product.selected_or_first_available_variant.available == false
                                or quantity_rule_soldout
                              %}
                                {{- 'sections.products.product.sold_out' | t -}}
                              {% else %}
                                {{- 'sections.products.product.add_to_cart' | t -}}
                              {% endif %}
                            </span>
                          </button>
                          {% if show_dynamic_checkout %}
                            <div class="button-actions">
                              {{ form | payment_button }}
                            </div>
                          {% endif %}
                        </div>
                      {%- endform -%}
                    </div>
                  {%- when 'pickup_availability' -%}
                    {%- style -%}
                      .main-product_{{ block.type }}-block{
                        --block-padding-top: {{ block.settings.block_top_padding }}px;
                        --block-padding-bottom: {{ block.settings.block_bottom_padding }}px;
                      }
                    {%- endstyle -%}
                    <div
                      class="mty main-product_type-block main-product_{{ block.type }}-block"
                      {{ block.shopify_attributes }}
                    >
                      <div data-pickup-bg-scheme="scheme--{{ section.settings.section_color_scheme }}" data-request-contents="pickup">
                        {%- assign pick_up_availabilities = product.selected_or_first_available_variant.store_availabilities
                          | where: 'pick_up_enabled', true
                        -%}
                        <div
                          {% if product.selected_or_first_available_variant.available
                            and pick_up_availabilities.size > 0
                          %}
                            available
                          {% else %}
                            hidden
                          {% endif %}
                        >
                          {%- liquid
                            assign closest_location = pick_up_availabilities.first
                          -%}
                          {%- if closest_location.available -%}
                            <strong class="pickup_heading">
                              {%- render 'icons-data', width: 10, height: 10, use_icon: true, icon_reference: 'check' -%}
                              {{- 'additions.product.pickup.available_at' | t }} {{ closest_location.location.name }}
                            </strong>
                            <em class="pickup_subheading">{{ closest_location.pick_up_time }}</em>
                          {%- else -%}
                            <strong class="pickup_heading">
                              {%- render 'icons-data', width: 10, height: 10, use_icon: true, icon_reference: 'cross' -%}
                              {{ 'sections.products.product.pickup.unavailable' | t }}: {{ closest_location.location.name }}
                            </strong>
                          {%- endif -%}
                        </div>
                        {% if product.selected_or_first_available_variant.available and pick_up_availabilities.size > 0 %}
                          <modal-component-toggler>
                            <button
                              type="button"
                              data-expanded="false"
                              data-modal-ref="modal-pickup-drawer"
                              aria-haspopup="dialog"
                              aria-controls="{{ section.id }}-modal-pickup-drawer"
                              class="pickup-drawer_btn-opener"
                            >
                              {% if pick_up_availabilities.size == 1 %}
                                {{ 'sections.products.product.pickup.view_store' | t }}
                              {% else %}
                                {{ 'sections.products.product.pickup.other_store' | t }}
                              {% endif %}
                            </button>
                          </modal-component-toggler>
                          <modal-component
                            data-modal="modal-pickup-drawer"
                            data-container="body"
                          >
                            <div
                              class="modal modal-component modal-component--pickup-drawer --drawer scheme--{{ section.settings.section_color_scheme }}"
                              role="dialog"
                              aria-modal="true"
                              aria-labelledby="{{ section.id }}-modal-pickup-drawer-heading"
                              aria-describedby="{{ section.id }}-modal-pickup-drawer"
                              tabindex="-1"
                            >
                              <div class="modal-component_wrapper" id="{{ section.id }}-modal-pickup-drawer">
                                <div class="modal-component_body">
                                  <div class="modal-component_head">
                                    <modal-component-toggler>
                                      <button type="button"
                                        data-expanded="true"
                                        data-toggle-attribute="false"
                                        data-modal-ref="modal-pickup-drawer"
                                        class="modal-component_btn-close"
                                      >
                                        {%- render 'icons-data', width: 14, height: 14, use_icon: true, icon_reference: 'cross' -%}
                                        <span class="visually-hidden">{{ 'accessibility.close' | t }}</span>
                                      </button>
                                    </modal-component-toggler>
                                    <strong id="{{ section.id }}-modal-pickup-drawer-heading" class="visually-hidden">{{ product.title | escape }}</strong>
                                  </div>
                                  <div class="modal-component_content" data-id="pickup">
                                    {%- render 'element-heading' | elem_heading_tag: 'h6' | elem_heading_text: product.title | elem_heading_custom_class: 'pick_up_availabilities-heading mty' -%}
                                    {%- render 'element-heading' | elem_heading_tag: 'span' | elem_heading_text: product.vendor | elem_heading_custom_class: 'pick_up_availabilities-vendor' -%}
                                    {%- unless product.has_only_default_variant -%}
                                      <ul class="list-unstyled mcpicku_options-list">
                                        {%- for option in product.options_with_values -%}
                                          <li>
                                            <strong class="mcpicku_options-list_title">{{ option.name }}:</strong>
                                            <strong class="mcpicku_options-list_value">
                                              {{ option.selected_value }}
                                            </strong>
                                          </li>
                                        {%- endfor -%}
                                      </ul>
                                    {%- endunless -%}
                                    <ul class="list-unstyled pickup-showcase_list">
                                      {%- for availability in pick_up_availabilities -%}
                                        <li>
                                          {%- render 'element-heading' | elem_heading_tag: 'strong' | elem_heading_parent: 'pickup-showcase' | elem_heading_text: availability.location.name | elem_heading_size: block.settings.heading_size | elem_heading_custom_class: 'mty' -%}
                                          <p class="availability-available">
                                            {%- if availability.available -%}
                                              <strong class="pickup-available_heading mty yes">
                                                {%- render 'icons-data', width: 10, height: 10, use_icon: true, icon_reference: 'check' -%}
                                                {{ 'sections.products.product.pickup.available' | t }}
                                              </strong>
                                              <span>{{ availability.pick_up_time }}</span>
                                            {%- else -%}
                                              <strong class="pickup-available_heading mty no">
                                                {%- render 'icons-data', width: 10, height: 10, use_icon: true, icon_reference: 'cross' -%}
                                                {{ 'sections.products.product.pickup.unavailable' | t }}
                                              </strong>
                                            {%- endif -%}
                                          </p>

                                          {%- assign address = availability.location.address -%}
                                          <address class="pickup-showcase_address">
                                            {{ address | format_address }}

                                            {%- if address.phone.size > 0 -%}
                                              <p>{{ address.phone }}</p>
                                            {%- endif -%}
                                          </address>
                                        </li>
                                      {%- endfor -%}
                                    </ul>
                                  </div>
                                </div>
                              </div>
                            </div>
                          </modal-component>
                        {% endif %}
                        <span class="--loader">
                          <span class="visually-hidden">{{ 'accessibility.loading' | t }}</span>
                          {%- render 'icons-data', width: 25, height: 25, use_icon: true, icon_reference: 'loader' -%}
                        </span>
                      </div>
                    </div>
                  {%- when 'message_box' -%}
                    {%- style -%}
                      .main-product_{{ block.type }}-block--{{ forloop.index }}{
                        --block-padding-top: {{ block.settings.block_top_padding }}px;
                        --block-padding-bottom: {{ block.settings.block_bottom_padding }}px;
                      }
                    {%- endstyle -%}
                    {% liquid
                      assign message_box_color_scheme = block.settings.message_box_color_scheme | prepend: 'scheme scheme--'
                    %}
                    <aside
                      class="mty main-product_type-block main-product_{{ block.type }}-block--{{ forloop.index }}"
                      {{ block.shopify_attributes }}
                    >
                      <div class="mty main-product_{{ block.type }}-block">
                        <div class="message-box {{ message_box_color_scheme }} {% if settings.enable_hover_anims %}anim--shine{% endif %}">
                          <div class="message-box_img">
                            {%- if block.settings.image != blank -%}
                              <img src="{{ block.settings.image | image_url: width: 350 }}" class="image-class" width="350" loading="lazy" height="350" alt="">
                              {%- else -%}
                                {{- 'product-3' | placeholder_svg_tag: 'img--placeholder' -}}
                            {%- endif -%}
                          </div>
                          <div class="message-box_description">
                            <strong class="mty message-box_heading">{{ block.settings.heading }}</strong>
                            <em class="mty message-box_subheading">{{ block.settings.subheading }}</em>
                            <div class="mty message-box_text">
                              {{ block.settings.description }}
                            </div>
                          </div>
                        </div>
                      </div>
                    </aside>
                  {%- when 'icon_with_description' -%}
                    {%- style -%}
                      .main-product_{{ block.type }}-block--{{ forloop.index }}{
                        --block-padding-top: {{ block.settings.block_top_padding }}px;
                        --block-padding-bottom: {{ block.settings.block_bottom_padding }}px;
                      }
                    {%- endstyle -%}
                    <div
                      class="mty main-product_type-block main-product_{{ block.type }}-block--{{ forloop.index }}"
                      {{ block.shopify_attributes }}
                    >
                      <div class="main-product_{{ block.type }}-block">
                        <aside class="ico-description-box">
                          <i class="ico-description-box_icn">
                            {%- if block.settings.image != blank -%}
                              <img src="{{ block.settings.image | image_url: width: 150 }}" class="image-class" width="150" loading="lazy" height="150" alt="">
                              {%- else -%}
                                {{- 'product-4' | placeholder_svg_tag: 'img--placeholder' -}}
                            {%- endif -%}
                          </i>
                          <div class="ico-description-box_description">
                            <strong class="ico-description-box_heading mty">{{ block.settings.heading }}</strong>
                            <div class="mty ico-description-box_text">
                              {{ block.settings.description }}
                            </div>
                          </div>
                        </aside>
                      </div>
                    </div>
                  {%- when 'collapsible_row' -%}
                    {%- style -%}
                      .main-product_{{ block.type }}-block--{{ forloop.index }}{
                        --block-padding-top: {{ block.settings.block_top_padding }}px;
                        --block-padding-bottom: {{ block.settings.block_bottom_padding }}px;
                      }
                    {%- endstyle -%}
                    <div
                      class="mty main-product_type-block main-product_{{ block.type }}-block--{{ forloop.index }}"
                      {{ block.shopify_attributes }}
                    >
                      <div class="main-product_{{ block.type }}-block">
                        {% liquid
                          assign accordion_prefix = 'main-product_accordion--'

                          assign defaul_section_open = ''
                          if block.settings.defaul_section_open
                            assign defaul_section_open = 'open'
                          endif
                        %}
                        <details class="collapsible-row {{ accordion_prefix }}row" {{ defaul_section_open }}>
                          <summary class="collapsible-row_opener {{ accordion_prefix }}opener">
                            {%- render 'element-heading' | elem_heading_tag: 'strong' | elem_heading_parent: 'collapsible-row' | elem_heading_text: block.settings.heading -%}
                            <i class="collapsible-row_icn">
                              <span class="{{ accordion_prefix }}icnopen">
                                {%- render 'icons-data', width: 10, height: 10, use_icon: true, icon_reference: 'plus' -%}
                              </span>
                              <span class="{{ accordion_prefix }}icnclose">
                                {%- render 'icons-data', width: 10, height: 10, use_icon: true, icon_reference: 'minus' -%}
                              </span>
                            </i>
                          </summary>
                          <div class="collapsible-row_content {{ accordion_prefix }}content">
                            {{ block.settings.description }}
                          </div>
                        </details>
                      </div>
                    </div>
                  {% when 'complementary' %}
                    {% unless block.settings.make_bundle_section %}
                      {%- style -%}
                        .main-product_{{ block.type }}-block{
                          --comp-padding-top: {{ block.settings.comp_top_padding }}px;
                          --comp-padding-bottom: {{ block.settings.comp_bottom_padding }}px;
                        }
                      {%- endstyle -%}
                      <div
                        class="mty main-product_type-block main-product_{{ block.type }}-block"
                        {{ block.shopify_attributes }}
                      >
                        <product-recommendations data-url="{{ routes.product_recommendations_url }}?section_id={{ section.id }}&product_id={{ product.id }}&limit=10&intent=complementary">
                          <div class="product-recommendations">
                            {%- if recommendations.performed and recommendations.products_count > 0 -%}
                              {{ 'product-complementary-row.css' | asset_url | stylesheet_tag }}
                              {%- render 'element-heading' | elem_heading_tag: 'h2' | elem_heading_parent: 'complementary-title' | elem_heading_text: block.settings.heading | elem_heading_size: block.settings.heading_size -%}
                              <splide-slider
                                data-drag="true"
                                data-autoplay="false"
                                data-gap="15px"
                                data-per-move="1"
                                data-type="slide"
                                data-arrows="{{ block.settings.comp_show_navigation }}"
                                data-pagination="false"
                                data-per-page="1"
                              >
                                <div class="splide collection-slider">
                                  <div class="splide__track">
                                    <div class="splide__list">
                                      {%- for product in recommendations.products -%}
                                        <div class="splide__slide">
                                          <article class="product-complementary-row {{ section.id }}-{{ product.id }}">
                                            {% render 'product-column-row',
                                              block: block,
                                              product: product,
                                              row_index: forloop.index
                                            %}
                                          </article>
                                        </div>
                                      {%- endfor -%}
                                    </div>
                                  </div>
                                </div>
                              </splide-slider>
                            {%- endif -%}
                          </div>
                        </product-recommendations>
                      </div>
                    {% endunless %}
                {%- endcase -%}
              {%- endfor -%}
            </div>
          </div>
        </div>
        <span class="{{ js_contents_prefix }}loader"> loading </span>
        {% liquid
          assign productJsonId = 'ProductCardJson-' | append: product.id | append: '-' | append: section.id
        %}
        <script id="{{ productJsonId }}" type="application/json">
          {{ product | json }}
        </script>
      </product-card>
    </div>
    {%- if section.settings.bottom_sticky_bar -%}
      <sticky-block
        class="main-product_sticky-bar"
        data-ref-block="#main-product-info"
        data-revealed="false"
        data-behavior="downwards"
        data-offset="100"
        aria-hidden="true"
        data-has-hidden="true"
        data-hide-on-end="true"
      >
        <div class="main-product_stacked-bar_wrapper">
          <div class="container">
            <div class="main-product_aside-card">
              {%- liquid
                if product.selected_or_first_available_variant.featured_media != null
                  assign media = product.selected_or_first_available_variant.featured_media

                else
                  for media in product.media
                    assign media = media
                    break
                  endfor
                endif
              -%}
              {% unless media == null %}
                <div class="main-product_aside_media-wrapper">
                  {% render 'element-image' | elem_image: media.preview_image | elem_image_class: 'image-class' | elem_image_placeholder: 'product-1' | elem_image_alt_text: product.title %}
                </div>
              {% endunless %}
              <div class="main-product_aside_actions-wrapper">
                <div class="col-left">
                  <div class="product-column-row_price mty">
                    {%- render 'price', product: product, use_variant: false -%}
                  </div>
                  {%- render 'element-heading' | elem_heading_tag: 'p' | elem_heading_parent: 'complementary-wrapper' | elem_heading_text: product.title | elem_heading_size: 'heading_xx_small' | elem_heading_custom_class: 'mty' -%}
                </div>
                <div class="col-right">
                  {% if section.settings.enable_stickybar_compare %}
                    <ul class="list-unstyled main-product_actions-list">
                      <li>
                        <product-compare-button
                          data-product="{{ product.title | escape }}"
                          data-handle="{{ product.handle }}"
                        >
                          <a href="#" role="button" data-title="{{- 'additions.product.compare.label' | t -}}" class="compare-btn">
                            <span class="visually-hidden pwb-text">
                              {{-
                                'sections.products.product.comparelist.add'
                                | t: product: product.title
                                | escape
                              -}}
                            </span>
                            {%- render 'icons-data', width: 20, height: 20, use_icon: true, icon_reference: 'compare' -%}
                          </a>
                        </product-compare-button>
                      </li>
                    </ul>
                  {% endif %}
                  <smooth-anchor data-offset=".sticky-header-wrapper">
                    <a class="button button--primary" href="#main-product-add-cart-ref">
                      {%- unless product.has_only_default_variant -%}
                        {%- render 'icons-data', width: 20, height: 20, use_icon: true, icon_reference: 'cart' -%}
                        {{- 'additions.product.choose.label' | t -}}
                      {% else %}
                        {{- 'sections.products.product.view_details_button' | t -}}
                      {% endunless %}
                    </a>
                  </smooth-anchor>
                </div>
              </div>
            </div>
          </div>
        </div>
      </sticky-block>
    {% endif %}
  </div>
  {%- for block in section.blocks -%}
    {%- case block.type -%}
      {% when 'complementary' %}
        {% liquid
          assign complementary_image_ratio_class = ''
          assign complementary_image_position_class = ''
          case block.settings.complementary_media_aspect_ratio
            when 'portrait'
              assign complementary_image_ratio_class = '--portrait-media'
              assign complementary_image_position_class = '--position-media'
            when 'square'
              assign complementary_image_ratio_class = '--square-media'
              assign complementary_image_position_class = '--position-media'
            when 'landscape'
              assign complementary_image_ratio_class = '--landscape-media'
              assign complementary_image_position_class = '--position-media'
            else
              assign complementary_image_ratio_class = '--adapt-media'
          endcase
          assign complementary_media_size_class = complementary_image_ratio_class
        %}
        {% if block.settings.make_bundle_section %}
          {%- style -%}
            bundle-product-wrapper{
              --comp-padding-top: {{ block.settings.comp_top_padding }}px;
              --comp-padding-bottom: {{ block.settings.comp_bottom_padding }}px;
            }
          {%- endstyle -%}
          <bundle-product-wrapper class="fbt-products-block {{ complementary_media_size_class }} {{ complementary_image_position_class }} scheme scheme--scheme1">
            <div class="container">
              {%- render 'element-heading' | elem_heading_tag: 'h3' | elem_heading_parent: 'complementary-wrapper' | elem_heading_text: block.settings.heading | elem_heading_size: block.settings.heading_size -%}
              <div class="fbt-products-block_wrapper">
                <div class="product-recommendations_wrap">
                  <product-recommendations data-url="{{ routes.product_recommendations_url }}?section_id={{ section.id }}&product_id={{ product.id }}&limit=10&intent=complementary">
                    <div class="product-recommendations">
                      {%- if recommendations.performed and recommendations.products_count > 0 -%}
                        {{ 'product-complementary-card.css' | asset_url | stylesheet_tag }}
                        <splide-slider
                          data-drag="true"
                          data-autoplay="false"
                          data-gap="15px"
                          data-per-move="1"
                          data-type="slide"
                          data-arrows="{{ section.settings.show_navigation }}"
                          data-pagination="false"
                          data-per-page="1"
                          data-breakpoints='{"responsive": {"576": {"perPage": 2},"992": {"perPage": 3},"1200": {"perPage": 4}}}'
                        >
                          <div class="splide collection-slider">
                            <div class="splide__track">
                              <div class="splide__list">
                                {%- for product in recommendations.products -%}
                                  {% liquid
                                    assign productJson = all_products[product]
                                    assign comp_js_contents_prefix = 'js-contents-complementary--'
                                    assign comp_product_form_id = section.id | append: product.id | append: forloop.index
                                    assign bundle_total_prcie = bundle_total_prcie | plus: product.selected_or_first_available_variant.price
                                  %}
                                  <div class="splide__slide {{ section.id }}-{{ product.id }}">
                                    <article class="product-complementary-card {{ section.id }}-{{ product.id }}">
                                      {% render 'product-complementary-card',
                                        product: product,
                                        product_form_id: comp_product_form_id,
                                        js_contents_prefix: comp_js_contents_prefix,
                                        productJson: productJson,
                                        column_index: forloop.index
                                      %}
                                    </article>
                                  </div>
                                {%- endfor -%}
                                <div class="splide__slide empty"></div>
                              </div>
                            </div>
                          </div>
                        </splide-slider>
                      {% endif %}
                    </div>
                  </product-recommendations>
                </div>
                <aside class="fbt-products-block_sidewrap visually-hidden --align-center">
                  <div class="thumb-image-list">
                    <ul class="list-unstyled fbt-thumbnails_list fbt-items-list">
                      {% comment %} thumbnails will be rendered here {% endcomment %}
                    </ul>
                  </div>
                  <div class="ftb_bottom-wrap">
                    <strong class="bundle-total-price-wrapper mty">
                      <span class="bundle-total-text">Total Price</span>
                      <span class="ftb_price">
                        <span class="bundle-price">
                          {{ 0 | money }}
                        </span>
                        <s class="bundle-compare-price visually-hidden">
                          {{ 0 | money }}
                        </s>
                      </span>
                      {% if settings.show_currency_code %}
                        <small class="bundle-curr-code">{{ localization.country.currency.iso_code }}</small>
                      {% endif %}
                    </strong>
                    <a href="#" class="bundle-cart-sbumit button button--primary disabled mty">
                      {%- render 'icons-data', width: 20, height: 20, use_icon: true, icon_reference: 'cart' -%}
                      <span class="hidden bcs-counter-wrap"><span class="bcs-count">0</span> item(s) </span>
                      Add to cart
                      <span class="--loader">
                        <span class="visually-hidden">{{ 'accessibility.loading' | t }}</span>
                        {%- render 'icons-data', width: 25, height: 25, use_icon: true, icon_reference: 'loader' -%}
                      </span>
                    </a>
                    {% if block.settings.bundle_dicount_text != empty %}
                      <div class="ftb_bottom-discount-text mty">
                        <p>{{ block.settings.bundle_dicount_text }}</p>
                      </div>
                    {% endif %}
                  </div>
                </aside>
              </div>
              <div hidden role="alert" class="error-toast"></div>
            </div>
          </bundle-product-wrapper>
        {% endif %}
    {% endcase %}
  {% endfor %}
  <script type="application/ld+json">
    {
      "@context": "http://schema.org/",
      "@type": "Product",
      "name": {{ product.title | json }},
      "url": {{ request.origin | append: product.url | json }},
      {% if seo_media -%}
        "image": [
          {{ seo_media | image_url: width: 1920 | prepend: "https:" | json }}
        ],
      {%- endif %}
      "description": {{ product.description | strip_html | json }},
      {% if product.selected_or_first_available_variant.sku != blank -%}
        "sku": {{ product.selected_or_first_available_variant.sku | json }},
      {%- endif %}
      "brand": {
        "@type": "Brand",
        "name": {{ product.vendor | json }}
      },
      "offers": [
        {%- for variant in product.variants -%}
          {
            "@type" : "Offer",
            {%- if variant.sku != blank -%}
              "sku": {{ variant.sku | json }},
            {%- endif -%}
            {%- if variant.barcode.size == 12 -%}
              "gtin12": {{ variant.barcode }},
            {%- endif -%}
            {%- if variant.barcode.size == 13 -%}
              "gtin13": {{ variant.barcode }},
            {%- endif -%}
            {%- if variant.barcode.size == 14 -%}
              "gtin14": {{ variant.barcode }},
            {%- endif -%}
            "availability" : "http://schema.org/{% if variant.available %}InStock{% else %}OutOfStock{% endif %}",
            "price" : {{ variant.price | divided_by: 100.00 | json }},
            "priceCurrency" : {{ cart.currency.iso_code | json }},
            "url" : {{ request.origin | append: variant.url | json }}
          }{% unless forloop.last %},{% endunless %}
        {%- endfor -%}
      ]
    }
  </script>
</div>

{%- if first_3d_model -%}
  <script type="application/json" id="ProductJSON-{{ product.id }}">
    {{ product.media | where: 'media_type', 'model' | json }}
  </script>
  <script src="{{ 'product-model.js' | asset_url }}" defer></script>
{%- endif -%}

{% if request.design_mode == false %}
  {% if product.media != null %}
    <script type="module">
      import PhotoSwipeLightbox from '{{ 'photoswipe-lightbox.esm.js' | asset_url }}';
      const options = {
        gallery: '#gallery-zoom-in-{{ section.id }}{{ product.id }}',
        children: '.--media-zoom-in',
        pswpModule: () => import('{{ 'photoswipe.esm.js' | asset_url }}')
      };
      const lightbox = new PhotoSwipeLightbox(options);
      lightbox.init();
    </script>
  {% endif %}
{% endif %}

{% schema %}
{
  "name": "t:sections.main_product.name",
  "tag": "section",
  "blocks": [
    {
      "type": "@app"
    },
    {
      "type": "custom_liquid",
      "name": "t:sections.common.custom_liquid.name",
      "settings": [
        {
          "type": "liquid",
          "id": "custom_liquid",
          "label": "t:sections.common.custom_liquid.label",
          "info": "t:sections.common.custom_liquid.info"
        },
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.text.settings.spacing.content"
        },
        {
          "type": "range",
          "id": "block_top_padding",
          "label": "t:sections.main_product.blocks.text.settings.spacing.p_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "block_bottom_padding",
          "label": "t:sections.main_product.blocks.text.settings.spacing.p_bottom.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 10,
          "unit": "px"
        }
      ]
    },
    {
      "type": "text",
      "name": "t:sections.main_product.blocks.text.name",
      "settings": [
        {
          "type": "inline_richtext",
          "id": "text",
          "default": "Share information about your products with your customers.",
          "label": "t:sections.main_product.blocks.text.label"
        },
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.text.settings.spacing.content"
        },
        {
          "type": "range",
          "id": "block_top_padding",
          "label": "t:sections.main_product.blocks.text.settings.spacing.p_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "block_bottom_padding",
          "label": "t:sections.main_product.blocks.text.settings.spacing.p_bottom.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 10,
          "unit": "px"
        }
      ]
    },
    {
      "type": "title",
      "name": "t:sections.main_product.blocks.title.name",
      "limit": 1,
      "settings": [
        {
          "type": "select",
          "id": "title_size",
          "label": "t:sections.main_product.blocks.title.settings.size.label",
          "options": [
            {
              "value": "heading_xx_large",
              "label": "t:sections.common.heading.size.options.opt_1.label"
            },
            {
              "value": "heading_x_large",
              "label": "t:sections.common.heading.size.options.opt_2.label"
            },
            {
              "value": "heading_large",
              "label": "t:sections.common.heading.size.options.opt_3.label"
            },
            {
              "value": "heading_medium",
              "label": "t:sections.common.heading.size.options.opt_4.label"
            },
            {
              "value": "heading_small",
              "label": "t:sections.common.heading.size.options.opt_5.label"
            },
            {
              "value": "heading_x_small",
              "label": "t:sections.common.heading.size.options.opt_6.label"
            },
            {
              "value": "heading_xx_small",
              "label": "t:sections.common.heading.size.options.opt_7.label"
            }
          ],
          "default": "heading_large"
        },
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.title.settings.spacing.content"
        },
        {
          "type": "range",
          "id": "block_top_padding",
          "label": "t:sections.main_product.blocks.title.settings.spacing.p_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "block_bottom_padding",
          "label": "t:sections.main_product.blocks.title.settings.spacing.p_bottom.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 10,
          "unit": "px"
        }
      ]
    },
    {
      "type": "statistics_bar",
      "name": "t:sections.main_product.blocks.stats_bar.name",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_savings_badge",
          "label": "t:sections.common.show_saving_badge.label",
          "default": true
        },
        {
          "type": "checkbox",
          "id": "show_inventory",
          "label": "t:sections.common.show_inventory.label",
          "default": true
        },
        {
          "type": "checkbox",
          "id": "show_rating",
          "label": "t:sections.common.show_rating.label",
          "default": true,
          "info": "t:sections.common.show_rating.info"
        },
        {
          "type": "checkbox",
          "id": "enable_compare_button",
          "label": "t:sections.common.enable_compare.label",
          "default": true
        },
        {
          "type": "checkbox",
          "id": "enable_share",
          "label": "t:sections.main_product.blocks.stats_bar.settings.share.label",
          "default": true
        },
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.stats_bar.settings.spacing.content"
        },
        {
          "type": "range",
          "id": "block_top_padding",
          "label": "t:sections.main_product.blocks.stats_bar.settings.spacing.p_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "block_bottom_padding",
          "label": "t:sections.main_product.blocks.stats_bar.settings.spacing.p_bottom.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 10,
          "unit": "px"
        }
      ]
    },
    {
      "type": "description",
      "name": "t:sections.main_product.blocks.description.name",
      "limit": 1,
      "settings": [
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.description.settings.spacing.content"
        },
        {
          "type": "range",
          "id": "block_top_padding",
          "label": "t:sections.main_product.blocks.description.settings.spacing.p_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "block_bottom_padding",
          "label": "t:sections.main_product.blocks.description.settings.spacing.p_bottom.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 10,
          "unit": "px"
        }
      ]
    },
    {
      "type": "stock_statistics_bar",
      "name": "t:sections.main_product.blocks.stock_bar.name",
      "limit": 1,
      "settings": [
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.stock_bar.content"
        },
        {
          "type": "checkbox",
          "id": "enable_sale_timer",
          "label": "t:sections.common.count_timer.label",
          "default": true
        },
        {
          "type": "text",
          "id": "sale_end_date",
          "label": "t:sections.common.count_timer.count_timer_end.label",
          "info": "t:sections.common.count_timer.count_timer_end.info",
          "default": "2025/04/05"
        },
        {
          "type": "inline_richtext",
          "id": "end_message",
          "label": "t:sections.common.count_timer.count_message.label",
          "info": "t:sections.common.count_timer.count_message.info"
        },
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.stock_bar.settings.spacing.content"
        },
        {
          "type": "range",
          "id": "block_top_padding",
          "label": "t:sections.main_product.blocks.stock_bar.settings.spacing.p_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "block_bottom_padding",
          "label": "t:sections.main_product.blocks.stock_bar.settings.spacing.p_bottom.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 10,
          "unit": "px"
        }
      ]
    },
    {
      "type": "price_and_variations",
      "name": "t:sections.main_product.blocks.price_variations.name",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_variations",
          "label": "t:sections.main_product.blocks.price_variations.label",
          "default": true
        },
        {
          "type": "checkbox",
          "id": "show_sku",
          "label": "t:sections.main_product.blocks.price_variations.settings.sku.label",
          "default": true
        },
        {
          "type": "checkbox",
          "id": "enable_ask_question",
          "label": "t:sections.main_product.blocks.price_variations.settings.question.label",
          "default": true
        },
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.price_variations.settings.chart.content"
        },
        {
          "type": "checkbox",
          "id": "enable_size_chart",
          "label": "t:sections.main_product.blocks.price_variations.settings.chart.label",
          "default": true
        },
        {
          "type": "image_picker",
          "id": "size_chart_image",
          "label": "t:sections.common.image.label"
        },
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.price_variations.settings.spacing.content"
        },
        {
          "type": "range",
          "id": "block_top_padding",
          "label": "t:sections.main_product.blocks.price_variations.settings.spacing.p_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "block_bottom_padding",
          "label": "t:sections.main_product.blocks.price_variations.settings.spacing.p_bottom.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 10,
          "unit": "px"
        }
      ]
    },
    {
      "type": "quantity_input",
      "name": "t:sections.main_product.blocks.qty_input.name",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_subtotals",
          "label": "t:sections.main_product.blocks.qty_input.settings.subtotals.label",
          "default": true
        },
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.qty_input.settings.spacing.content"
        },
        {
          "type": "range",
          "id": "block_top_padding",
          "label": "t:sections.main_product.blocks.qty_input.settings.spacing.p_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "block_bottom_padding",
          "label": "t:sections.main_product.blocks.qty_input.settings.spacing.p_bottom.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 10,
          "unit": "px"
        }
      ]
    },
    {
      "type": "buttons",
      "name": "t:sections.main_product.blocks.buttons.name",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_dynamic_checkouts",
          "default": true,
          "label": "t:sections.main_product.blocks.buttons.settings.dynamic.label",
          "info": "t:sections.main_product.blocks.buttons.settings.dynamic.info"
        },
        {
          "type": "checkbox",
          "id": "show_gift_card_recipient",
          "default": false,
          "label": "t:sections.main_product.blocks.buttons.settings.gift.label",
          "info": "t:sections.main_product.blocks.buttons.settings.gift.info"
        },
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.buttons.settings.spacing.content"
        },
        {
          "type": "range",
          "id": "block_top_padding",
          "label": "t:sections.main_product.blocks.buttons.settings.spacing.p_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "block_bottom_padding",
          "label": "t:sections.main_product.blocks.buttons.settings.spacing.p_bottom.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 10,
          "unit": "px"
        }
      ]
    },
    {
      "type": "pickup_availability",
      "name": "Pickup",
      "limit": 1,
      "settings": [
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.pickup.settings.spacing.content"
        },
        {
          "type": "range",
          "id": "block_top_padding",
          "label": "t:sections.main_product.blocks.pickup.settings.spacing.p_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "block_bottom_padding",
          "label": "t:sections.main_product.blocks.pickup.settings.spacing.p_bottom.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 10,
          "unit": "px"
        }
      ]
    },
    {
      "type": "message_box",
      "name": "t:sections.main_product.blocks.message_box.name",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "t:sections.common.image.label"
        },
        {
          "type": "inline_richtext",
          "id": "heading",
          "label": "t:sections.common.heading.label",
          "default": "Message box"
        },
        {
          "type": "inline_richtext",
          "id": "subheading",
          "label": "t:sections.common.subheading.label",
          "default": "Stay Informed"
        },
        {
          "type": "inline_richtext",
          "id": "description",
          "label": "t:sections.common.description.label",
          "default": "Find all the details you need about our products and services. We're here to make your shopping experience hassle-free."
        },
        {
          "type": "header",
          "content": "t:sections.common.style.content"
        },
        {
          "type": "color_scheme",
          "id": "message_box_color_scheme",
          "default": "scheme4",
          "label": "t:sections.common.clr_scheme.label"
        },
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.message_box.settings.spacing.content"
        },
        {
          "type": "range",
          "id": "block_top_padding",
          "label": "t:sections.main_product.blocks.message_box.settings.spacing.p_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "block_bottom_padding",
          "label": "t:sections.main_product.blocks.message_box.settings.spacing.p_bottom.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 10,
          "unit": "px"
        }
      ]
    },
    {
      "type": "icon_with_description",
      "name": "t:sections.main_product.blocks.ico_description.name",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "t:sections.common.image.label"
        },
        {
          "type": "inline_richtext",
          "id": "heading",
          "label": "t:sections.common.heading.label",
          "default": "Icon with description"
        },
        {
          "type": "inline_richtext",
          "id": "description",
          "label": "t:sections.common.description.label",
          "default": "Pair text with an icon to focus on your product unique selling feature."
        },
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.ico_description.settings.spacing.content"
        },
        {
          "type": "range",
          "id": "block_top_padding",
          "label": "t:sections.main_product.blocks.ico_description.settings.spacing.p_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "block_bottom_padding",
          "label": "t:sections.main_product.blocks.ico_description.settings.spacing.p_bottom.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 10,
          "unit": "px"
        }
      ]
    },
    {
      "type": "collapsible_row",
      "name": "t:sections.main_product.blocks.collapse_row.name",
      "settings": [
        {
          "type": "inline_richtext",
          "id": "heading",
          "label": "t:sections.common.heading.label",
          "default": "Collapsible row"
        },
        {
          "type": "inline_richtext",
          "id": "description",
          "label": "t:sections.common.description.label",
          "default": "Collapsible description"
        },
        {
          "type": "checkbox",
          "id": "defaul_section_open",
          "default": false,
          "label": "t:sections.main_product.blocks.collapse_row.settings.open.label"
        },
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.collapse_row.settings.spacing.content"
        },
        {
          "type": "range",
          "id": "block_top_padding",
          "label": "t:sections.main_product.blocks.collapse_row.settings.spacing.p_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "block_bottom_padding",
          "label": "t:sections.main_product.blocks.collapse_row.settings.spacing.p_bottom.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 10,
          "unit": "px"
        }
      ]
    },
    {
      "type": "complementary",
      "name": "t:sections.main_product.blocks.complementary.name",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "t:sections.main_product.blocks.complementary.content"
        },
        {
          "type": "inline_richtext",
          "id": "heading",
          "label": "t:sections.common.heading.label",
          "default": "Frequently bought together"
        },
        {
          "type": "select",
          "id": "heading_size",
          "label": "t:sections.common.heading.size.label",
          "options": [
            {
              "value": "heading_xx_large",
              "label": "t:sections.common.heading.size.options.opt_1.label"
            },
            {
              "value": "heading_x_large",
              "label": "t:sections.common.heading.size.options.opt_2.label"
            },
            {
              "value": "heading_large",
              "label": "t:sections.common.heading.size.options.opt_3.label"
            },
            {
              "value": "heading_medium",
              "label": "t:sections.common.heading.size.options.opt_4.label"
            },
            {
              "value": "heading_small",
              "label": "t:sections.common.heading.size.options.opt_5.label"
            },
            {
              "value": "heading_x_small",
              "label": "t:sections.common.heading.size.options.opt_6.label"
            },
            {
              "value": "heading_xx_small",
              "label": "t:sections.common.heading.size.options.opt_7.label"
            }
          ],
          "default": "heading_x_small"
        },
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.complementary.settings.bundle.content",
          "info": "t:sections.main_product.blocks.complementary.settings.bundle.info"
        },
        {
          "type": "checkbox",
          "id": "make_bundle_section",
          "default": false,
          "label": "t:sections.main_product.blocks.complementary.settings.bundle.label"
        },
        {
          "type": "textarea",
          "id": "bundle_dicount_text",
          "label": "t:sections.main_product.blocks.complementary.settings.bundle.discount.label"
        },
        {
          "type": "select",
          "id": "complementary_media_aspect_ratio",
          "label": "t:sections.common.image.ratio.label",
          "info": "t:sections.common.image.ratio.info",
          "options": [
            {
              "value": "adapt_to_image",
              "label": "t:sections.common.image.ratio.options.opt_1.label"
            },
            {
              "value": "portrait",
              "label": "t:sections.common.image.ratio.options.opt_2.label"
            },
            {
              "value": "square",
              "label": "t:sections.common.image.ratio.options.opt_3.label"
            },
            {
              "value": "landscape",
              "label": "t:sections.common.image.ratio.options.opt_4.label"
            }
          ],
          "default": "adapt_to_image"
        },
        {
          "type": "header",
          "content": "t:sections.common.slideshow.content",
          "info": "t:sections.main_product.blocks.complementary.settings.slideshow.info"
        },
        {
          "type": "checkbox",
          "id": "comp_show_navigation",
          "label": "t:sections.common.slideshow.show_arrows.label",
          "default": true,
          "info": "t:sections.common.slideshow.show_arrows.info"
        },
        {
          "type": "header",
          "content": "t:sections.main_product.blocks.complementary.settings.spacing.content"
        },
        {
          "type": "range",
          "id": "comp_top_padding",
          "label": "t:sections.main_product.blocks.complementary.settings.spacing.p_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 50,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "comp_bottom_padding",
          "label": "t:sections.main_product.blocks.complementary.settings.spacing.p_bottom.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 50,
          "unit": "px"
        }
      ]
    }
  ],
  "settings": [
    {
      "type": "checkbox",
      "id": "sticky_product",
      "default": true,
      "label": "t:sections.main_product.settings.sticky_info.label",
      "info": "t:sections.main_product.settings.sticky_info.info"
    },
    {
      "type": "header",
      "content": "t:sections.main_product.settings.media.content",
      "info": "t:sections.main_product.settings.media.info"
    },
    {
      "type": "select",
      "id": "gallery_view",
      "label": "t:sections.main_product.settings.gallery.label",
      "options": [
        {
          "value": "full_two_thumbs",
          "label": "t:sections.main_product.settings.gallery.options.opt_1.label"
        },
        {
          "value": "full_thumb",
          "label": "t:sections.main_product.settings.gallery.options.opt_2.label"
        },
        {
          "value": "slider_with_thumbs",
          "label": "t:sections.main_product.settings.gallery.options.opt_3.label"
        },
        {
          "value": "slider_full",
          "label": "t:sections.main_product.settings.gallery.options.opt_4.label"
        }
      ],
      "default": "slider_with_thumbs"
    },
    {
      "type": "select",
      "id": "media_width",
      "label": "t:sections.main_product.settings.media.width.label",
      "info": "t:sections.main_product.settings.media.width.info",
      "options": [
        {
          "value": "large",
          "label": "t:sections.main_product.settings.media.width.options.opt_1.label"
        },
        {
          "value": "medium",
          "label": "t:sections.main_product.settings.media.width.options.opt_2.label"
        },
        {
          "value": "small",
          "label": "t:sections.main_product.settings.media.width.options.opt_3.label"
        }
      ],
      "default": "medium"
    },
    {
      "type": "select",
      "id": "media_aspect_ratio",
      "label": "t:sections.common.image.ratio.label",
      "info": "t:sections.common.image.ratio.info",
      "options": [
        {
          "value": "adapt_to_image",
          "label": "t:sections.common.image.ratio.options.opt_1.label"
        },
        {
          "value": "portrait",
          "label": "t:sections.common.image.ratio.options.opt_2.label"
        },
        {
          "value": "square",
          "label": "t:sections.common.image.ratio.options.opt_3.label"
        },
        {
          "value": "landscape",
          "label": "t:sections.common.image.ratio.options.opt_4.label"
        }
      ],
      "default": "adapt_to_image"
    },
    {
      "type": "header",
      "content": "t:sections.main_product.settings.bottom_sticky_bar.content"
    },
    {
      "type": "checkbox",
      "id": "bottom_sticky_bar",
      "default": true,
      "label": "t:sections.main_product.settings.bottom_sticky_bar.label"
    },
    {
      "type": "checkbox",
      "id": "enable_stickybar_compare",
      "label": "t:sections.common.enable_compare.label",
      "default": true
    },
    {
      "type": "header",
      "content": "t:sections.common.style.content"
    },
    {
      "type": "color_scheme",
      "id": "section_color_scheme",
      "default": "scheme1",
      "label": "t:sections.common.clr_scheme.label"
    },
    {
      "type": "header",
      "content": "t:sections.common.slideshow.content",
      "info": "t:sections.main_product.settings.slideshow.info"
    },
    {
      "type": "checkbox",
      "id": "show_navigation",
      "label": "t:sections.common.slideshow.show_arrows.label",
      "default": true,
      "info": "t:sections.common.slideshow.show_arrows.info"
    },
    {
      "type": "select",
      "id": "pagination",
      "label": "t:sections.common.slideshow.show_nav.label",
      "options": [
        {
          "value": "none",
          "label": "t:sections.common.slideshow.show_nav.options.opt_1.label"
        },
        {
          "value": "dots",
          "label": "t:sections.common.slideshow.show_nav.options.opt_2.label"
        },
        {
          "value": "lines",
          "label": "t:sections.common.slideshow.show_nav.options.opt_3.label"
        },
        {
          "value": "numbers",
          "label": "t:sections.common.slideshow.show_nav.options.opt_4.label"
        }
      ],
      "default": "lines"
    },
    {
      "type": "header",
      "content": "t:sections.common.spacing.content"
    },
    {
      "type": "range",
      "id": "section_top_padding",
      "label": "t:sections.common.spacing.top_spac.label",
      "min": 0,
      "max": 100,
      "step": 1,
      "default": 50,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "section_bottom_padding",
      "label": "t:sections.common.spacing.bottom_spac.label",
      "min": 0,
      "max": 100,
      "step": 1,
      "default": 50,
      "unit": "px"
    }
  ]
}
{% endschema %}
